{
  "name": "fx-wallet-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack FX multi-currency wallet and conversion platform portfolio project.",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "seed": "npm run seed --workspace server",
    "server": "npm run dev --workspace server",
    "client": "npm run dev --workspace client"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
